{
  "scripts": {
    "test": "python -m http.server 2>/dev/null & SH_PID=$!; EXIT_CODE=0; playwright test tests/ || EXIT_CODE=$?; kill $SH_PID 2>/dev/null; exit $EXIT_CODE"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
